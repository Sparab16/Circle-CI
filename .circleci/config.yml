version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run:
          name: The first step
          command: |
            echo 'Hey There'
            echo 'This is the delivery pipeline from Shreyas'
      - run:
          name: Code has arrived
          command: |
            ls -al
            echo '^^^That should look familiar^^^'